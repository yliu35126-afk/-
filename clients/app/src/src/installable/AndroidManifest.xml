<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <application android:usesCleartextTraffic="true" android:networkSecurityConfig="@xml/network_security_config_installable" tools:replace="android:networkSecurityConfig,android:usesCleartextTraffic">
        <!-- 为 installable 变体显式声明注册入口页 -->
        <activity
            android:name="com.example.myapplication.RegistrationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 主界面保留但不作为桌面入口 -->
        <activity
            android:name="com.example.myapplication.MainActivity"
            android:exported="true" />
    </application>
</manifest>
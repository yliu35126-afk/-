package com.example.myapplication

object Defaults {
    // 统一默认兜底入口（与后端保持一致）
    const val FALLBACK_DEVICE_ID = "DEV_TEST_DEFAULT"
    const val FALLBACK_CONFIG_ID = "26"
    const val FALLBACK_PRIZE_COUNT = 16
    const val FALLBACK_ACTIVITY_PRICE = 0.01
}